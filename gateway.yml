spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        - id: user-notif-service
          uri: lb://user-notif-service
          predicates:
            - Path=/api/auth/**
            #- Path=/api/users/**
        - id: lamain-service
          uri: lb://lamain-service
          predicates:
            - Path=/api/lamain/**
